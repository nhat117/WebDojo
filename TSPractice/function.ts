/* Function */

// const square_calc = (side: number) => side *side

// console.log(square_calc(2));

// const add = (a:number, b: number, c?: number|string) => a + b + c;
// console.log(add(2,3,5));

// const add_default = (a:number, b: number, c: number|string = 10) => a + b + c;

// console.log(add_default(2,3));

const minus = (a: number, b: number): number => a - b;

console.log(minus(10,7));